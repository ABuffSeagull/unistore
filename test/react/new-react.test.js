import React from 'react';
import { createIntegration } from '../../src/index.js';

const { useStore } = createIntegration(React);

describe('useStore', () => {
  it('should return an array', () => {

  });
});
